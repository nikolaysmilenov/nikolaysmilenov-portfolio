# Project preview images

Optional screenshots for project cards.

Example:
1. Add `satovcha-info.png` here
2. Set `previewImage: "satovcha-info.png"` in `src/data/projects.ts`

Until then, elegant placeholders are shown automatically.
